<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>todolist</title>
</head>
<body>
    <h1>todo list</h1>
    <h3>Tâche</h3>
    <ul>
        <% tasks.forEach((task) => { %> 
            <% if (task.done) {%>
                <li>
                    <%= task.title %> | <a href="#">terminer</a>
                </li>
            <% } else { %>
                <li>
                    <s> <%= task.title %> </s> |  <a href="#">supprimer</a>
                </li>
                <% +} %>
        <% }) %>

    </ul>
</body>
</html>